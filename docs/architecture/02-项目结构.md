---
title: 项目结构
version: 1.0.0
lastUpdated: 2026-03-01
powerBy: Claude Code
---

# 项目结构

## 1. 解决方案概览

### 1.1 项目组成

SmallEBot 解决方案 (`SmallEBot.slnx`) 包含 4 个项目：

| 项目 | 类型 | 职责 |
|------|------|------|
| `SmallEBot.Core` | 类库 | 领域模型、实体、仓储接口 |
| `SmallEBot.Application` | 类库 | 业务接口、对话管道 |
| `SmallEBot.Infrastructure` | 类库 | 数据持久化、仓储实现 |
| `SmallEBot` | Web 应用 | Blazor Server 宿主、UI 组件 |

### 1.2 目录结构

```
SmallEBot/
├── SmallEBot.slnx                    # 解决方案文件
├── CLAUDE.md                         # 开发指南
│
├── SmallEBot.Core/                   # 核心层
│   ├── Entities/                     # 领域实体
│   ├── Models/                       # 模型类
│   └── Repositories/                 # 仓储接口
│
├── SmallEBot.Application/            # 应用层
│   ├── Context/                      # 上下文管理
│   ├── Conversation/                 # 对话服务
│   └── Streaming/                    # 流式接口
│
├── SmallEBot.Infrastructure/         # 基础设施层
│   ├── Data/                         # 数据库相关
│   │   ├── Migrations/               # EF Core 迁移
│   │   ├── SmallEBotDbContext.cs     # 数据库上下文
│   │   └── BackfillTurnsService.cs   # 数据回填服务
│   └── Repositories/                 # 仓储实现
│
├── SmallEBot/                        # 宿主层
│   ├── Components/                   # Blazor 组件
│   │   ├── Chat/                     # 聊天相关组件
│   │   ├── Layout/                   # 布局组件
│   │   ├── Pages/                    # 页面组件
│   │   ├── Workspace/                # 工作区组件
│   │   ├── Terminal/                 # 终端配置组件
│   │   ├── Mcp/                      # MCP 配置组件
│   │   ├── Skills/                   # 技能配置组件
│   │   ├── Agent/                    # Agent 配置组件
│   │   └── Settings/                 # 设置组件
│   ├── Services/                     # 服务实现
│   │   ├── Agent/                    # Agent 相关服务
│   │   ├── Circuit/                  # Blazor Circuit 管理
│   │   ├── Conversation/             # 对话相关服务
│   │   ├── Context/                  # 上下文管理
│   │   ├── Mcp/                      # MCP 服务
│   │   ├── Presentation/             # 展示层服务
│   │   ├── Skills/                   # 技能服务
│   │   ├── Streaming/                # 流式输出
│   │   ├── Terminal/                 # 终端服务
│   │   ├── User/                     # 用户服务
│   │   └── Workspace/                # 工作区服务
│   ├── Extensions/                   # 扩展方法
│   ├── Models/                       # 宿主层模型
│   ├── wwwroot/                      # 静态资源
│   ├── Program.cs                    # 入口点
│   └── App.razor                     # 根组件
│
└── docs/                             # 文档目录
    ├── architecture/                 # 架构文档
    └── plans/                        # 设计文档
```

---

## 2. 核心层 (SmallEBot.Core)

### 2.1 领域实体

| 文件 | 描述 |
|------|------|
| `Entities/Conversation.cs` | 会话实体，包含消息、工具调用、思考块 |
| `Entities/ChatMessage.cs` | 聊天消息实体 |
| `Entities/ToolCall.cs` | 工具调用记录 |
| `Entities/ThinkBlock.cs` | 思考块（Reasoning） |
| `Entities/ConversationTurn.cs` | 对话轮次 |

### 2.2 模型类

| 文件 | 描述 |
|------|------|
| `Models/StreamUpdate.cs` | 流式更新基类及子类 |
| `Models/ChatBubble.cs` | 聊天气泡视图模型 |
| `Models/AssistantSegment.cs` | 助手回复片段 |
| `Models/ContextUsageEstimate.cs` | 上下文使用估算 |
| `Models/ModelConfig.cs` | 模型配置 |
| `Models/TimelineItem.cs` | 时间线项 |

### 2.3 仓储接口

| 文件 | 描述 |
|------|------|
| `Repositories/IConversationRepository.cs` | 对话仓储接口 |

---

## 3. 应用层 (SmallEBot.Application)

### 3.1 对话服务

| 文件 | 描述 |
|------|------|
| `Conversation/IAgentConversationService.cs` | 对话服务接口 |
| `Conversation/AgentConversationService.cs` | 对话服务实现 |
| `Conversation/ICompressionService.cs` | 上下文压缩服务接口 |
| `Conversation/IContextUsageEstimator.cs` | 上下文估算接口 |

### 3.2 流式接口

| 文件 | 描述 |
|------|------|
| `Streaming/IAgentRunner.cs` | Agent 运行器接口 |
| `Streaming/IStreamSink.cs` | 流式输出接收接口 |

### 3.3 上下文管理

| 文件 | 描述 |
|------|------|
| `Context/IContextWindowManager.cs` | 上下文窗口管理接口 |

---

## 4. 基础设施层 (SmallEBot.Infrastructure)

### 4.1 数据库

| 文件 | 描述 |
|------|------|
| `Data/SmallEBotDbContext.cs` | EF Core 数据库上下文 |
| `Data/BackfillTurnsService.cs` | 历史数据回填服务 |

### 4.2 仓储实现

| 文件 | 描述 |
|------|------|
| `Repositories/ConversationRepository.cs` | 对话仓储实现 |

### 4.3 数据库迁移

| 迁移文件 | 描述 |
|----------|------|
| `InitialCreate` | 初始数据库结构 |
| `AddToolCalls` | 添加工具调用表 |
| `ToolCallToConversation` | 工具调用关联到会话 |
| `AddThinkBlocks` | 添加思考块表 |
| `AddConversationTurns` | 添加对话轮次表 |
| `FkNoAction` | 外键级联行为修改 |
| `AddMessageEditSupport` | 消息编辑支持 |
| `AddMessageAttachments` | 消息附件支持 |
| `AddCompressionFields` | 上下文压缩字段 |

---

## 5. 宿主层 (SmallEBot)

### 5.1 组件结构

#### 5.5.1 聊天组件

| 组件 | 描述 |
|------|------|
| `ChatArea.razor` | 聊天主界面编排器 |
| `MessageList.razor` | 消息列表渲染 |
| `ChatInputArea.razor` | 输入区域 |
| `ChatInputBar.razor` | 输入栏 |
| `StreamingIndicator.razor` | 流式输出指示器 |
| `StreamingMessageView.razor` | 流式消息视图 |
| `ReasoningBlockView.razor` | 推理块视图 |
| `ToolCallView.razor` | 工具调用视图 |
| `ConversationSidebar.razor` | 会话侧边栏 |
| `AttachmentPopover.razor` | 附件弹出框 |
| `AttachmentChips.razor` | 附件标签 |

#### 5.5.2 工作区组件

| 组件 | 描述 |
|------|------|
| `WorkspaceDrawer.razor` | 工作区抽屉 |
| `WorkspaceTreeItem.razor` | 工作区树项 |

#### 5.5.3 配置组件

| 组件 | 描述 |
|------|------|
| `TerminalConfigDialog.razor` | 终端配置对话框 |
| `McpConfigDialog.razor` | MCP 配置对话框 |
| `SkillsConfigDialog.razor` | 技能配置对话框 |
| `ModelConfigDialog.razor` | 模型配置对话框 |
| `SettingsDialog.razor` | 设置对话框 |

### 5.2 服务结构

#### 5.2.1 Agent 服务

| 文件 | 描述 |
|------|------|
| `AgentBuilder.cs` | 构建和缓存 AIAgent |
| `AgentRunnerAdapter.cs` | IAgentRunner 实现 |
| `AgentContextFactory.cs` | 系统提示词工厂 |
| `AgentCacheService.cs` | Agent 缓存服务 |
| `CompressionService.cs` | 上下文压缩实现 |
| `McpConnectionManager.cs` | MCP 连接管理 |
| `ModelConfigService.cs` | 模型配置服务 |

#### 5.2.2 工具提供者

| 文件 | 描述 |
|------|------|
| `IToolProvider.cs` | 工具提供者接口 |
| `ToolProviderAggregator.cs` | 工具聚合器 |
| `TimeToolProvider.cs` | 时间工具 |
| `FileToolProvider.cs` | 文件工具 |
| `SearchToolProvider.cs` | 搜索工具 |
| `ShellToolProvider.cs` | 终端工具 |
| `TaskToolProvider.cs` | 任务列表工具 |
| `SkillToolProvider.cs` | 技能工具 |
| `ConversationToolProvider.cs` | 对话工具 |
| `SkillGenerationToolProvider.cs` | 技能生成工具 |

#### 5.2.3 其他服务

| 目录 | 描述 |
|------|------|
| `Circuit/` | Blazor Circuit 上下文管理 |
| `Conversation/` | 对话相关服务 |
| `Context/` | 上下文窗口管理 |
| `Mcp/` | MCP 配置和工具加载 |
| `Presentation/` | 展示层服务（Markdown、快捷键） |
| `Skills/` | 技能配置和解析 |
| `Streaming/` | 流式输出实现 |
| `Terminal/` | 终端命令执行和确认 |
| `User/` | 用户偏好服务 |
| `Workspace/` | 工作区虚拟文件系统 |

---

## 6. 依赖注入配置

### 6.1 服务注册

所有服务在 `ServiceCollectionExtensions.cs` 中注册：

```csharp
// 数据库
services.AddDbContext<SmallEBotDbContext>();
services.AddScoped<IConversationRepository, ConversationRepository>();

// Application 层
services.AddScoped<IAgentConversationService, AgentConversationService>();
services.AddScoped<IAgentRunner, AgentRunnerAdapter>();

// Agent 服务
services.AddScoped<IAgentBuilder, AgentBuilder>();
services.AddScoped<IAgentContextFactory, AgentContextFactory>();
services.AddSingleton<IMcpConnectionManager, McpConnectionManager>();

// 工具提供者
services.AddSingleton<IToolProvider, TimeToolProvider>();
services.AddSingleton<IToolProvider, FileToolProvider>();
services.AddSingleton<IToolProvider, ShellToolProvider>();
// ... 更多工具

// UI 状态
services.AddScoped<ChatState>();
services.AddScoped<ChatPresentationService>();
```

### 6.2 服务生命周期

| 生命周期 | 服务类型 |
|----------|----------|
| **Singleton** | 配置服务、MCP 管理、虚拟文件系统 |
| **Scoped** | 对话服务、Agent 相关、UI 状态 |
| **Transient** | 无 |
