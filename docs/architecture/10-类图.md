---
title: 类图
version: 1.0.0
lastUpdated: 2026-03-01
powerBy: Claude Code
---

# 类图

## 1. 核心实体类图

### 1.1 会话聚合

```mermaid
classDiagram
    class Conversation {
        +Guid Id
        +string UserName
        +string Title
        +DateTime CreatedAt
        +DateTime UpdatedAt
        +string CompressedContext
        +DateTime CompressedAt
        +ICollection~ChatMessage~ Messages
        +ICollection~ToolCall~ ToolCalls
        +ICollection~ThinkBlock~ ThinkBlocks
        +ICollection~ConversationTurn~ Turns
    }

    class ConversationTurn {
        +Guid Id
        +Guid ConversationId
        +bool UseThinking
        +DateTime CreatedAt
    }

    class ChatMessage {
        +Guid Id
        +Guid ConversationId
        +Guid TurnId
        +string Role
        +string Content
        +DateTime CreatedAt
        +string Attachments
        +string RequestedSkills
        +bool IsEdited
    }

    class ToolCall {
        +Guid Id
        +Guid ConversationId
        +Guid TurnId
        +string ToolName
        +string CallId
        +string Arguments
        +string Result
        +int SortOrder
        +DateTime CreatedAt
    }

    class ThinkBlock {
        +Guid Id
        +Guid ConversationId
        +Guid TurnId
        +string Content
        +int SortOrder
        +DateTime CreatedAt
    }

    Conversation "1" *-- "0..*" ChatMessage : contains
    Conversation "1" *-- "0..*" ToolCall : contains
    Conversation "1" *-- "0..*" ThinkBlock : contains
    Conversation "1" *-- "0..*" ConversationTurn : contains
    ConversationTurn "1" *-- "0..1" ChatMessage : userMessage
    ConversationTurn "1" *-- "0..1" ChatMessage : assistantMessage
    ConversationTurn "1" *-- "0..*" ToolCall : contains
    ConversationTurn "1" *-- "0..*" ThinkBlock : contains
```

---

## 2. 流式更新类图

### 2.1 StreamUpdate 层次

```mermaid
classDiagram
    class StreamUpdate {
        <<abstract record>>
    }

    class TextStreamUpdate {
        +string Text
    }

    class ThinkStreamUpdate {
        +string Text
    }

    class ToolCallStreamUpdate {
        +string ToolName
        +string CallId
        +ToolCallPhase Phase
        +string Arguments
        +string Result
        +TimeSpan Elapsed
    }

    class ToolCallPhase {
        <<enumeration>>
        Started
        Completed
    }

    StreamUpdate <|-- TextStreamUpdate
    StreamUpdate <|-- ThinkStreamUpdate
    StreamUpdate <|-- ToolCallStreamUpdate
    ToolCallStreamUpdate --> ToolCallPhase : phase
```

### 2.2 模型类

```mermaid
classDiagram
    class ChatBubble {
        +Guid Id
        +bool IsUser
        +string Content
        +DateTime CreatedAt
        +IReadOnlyList~string~ AttachedPaths
        +IReadOnlyList~string~ RequestedSkillIds
        +IReadOnlyList~ToolCall~ ToolCalls
        +IReadOnlyList~ThinkBlock~ ThinkBlocks
    }

    class AssistantSegment {
        <<record>>
        +bool IsText
        +bool IsThink
        +string Content
    }

    class ContextUsageEstimate {
        +int InputTokens
        +int ContextWindowTokens
        +double Ratio
        +string Summary
    }

    class ModelConfig {
        +string Name
        +string Model
        +string BaseUrl
        +string ApiKeySource
        +int ContextWindow
        +bool IsDefault
    }

    class TimelineItem {
        +Guid Id
        +TimelineItemType Type
        +DateTime CreatedAt
        +string Content
    }

    class ICreateTime {
        <<interface>>
        +DateTime CreatedAt
    }

    ChatBubble ..|> ICreateTime
```

---

## 3. 应用层接口类图

### 3.1 核心接口

```mermaid
classDiagram
    class IAgentConversationService {
        <<interface>>
        +CreateConversationAsync() Task~Conversation~
        +GetConversationsAsync() Task~List~Conversation~~
        +GetConversationAsync() Task~Conversation?~
        +DeleteConversationAsync() Task~bool~
        +CreateTurnAndUserMessageAsync() Task~Guid~
        +StreamResponseAndCompleteAsync() Task
        +CompleteTurnWithAssistantAsync() Task
        +CompleteTurnWithErrorAsync() Task
        +ReplaceUserMessageAsync() Task
        +RegenerateAsync() Task
        +CompactConversationAsync() Task~bool~
        +CheckAndCompactIfNeededAsync() Task~bool~
        +event CompressionStarted
        +event CompressionCompleted
    }

    class IAgentRunner {
        <<interface>>
        +RunStreamingAsync() IAsyncEnumerable~StreamUpdate~
        +GenerateTitleAsync() Task~string~
    }

    class IStreamSink {
        <<interface>>
        +OnNextAsync() ValueTask
    }

    class ICompressionService {
        <<interface>>
        +GenerateSummaryAsync() Task~string?~
    }

    class IContextUsageEstimator {
        <<interface>>
        +GetEstimatedContextUsageDetailAsync() Task~ContextUsageEstimate?~
    }

    class IContextWindowManager {
        <<interface>>
        +TrimToFit() TrimResult
    }

    class ICommandConfirmationContext {
        <<interface>>
        +SetCurrentId() void
        +GetCurrentId() string?
    }

    class IConversationTaskContext {
        <<interface>>
        +SetConversationId() void
        +GetConversationId() Guid?
    }

    IAgentConversationService --> IAgentRunner : uses
    IAgentConversationService --> IStreamSink : writes to
    IAgentConversationService --> ICompressionService : uses
    IAgentConversationService --> IContextUsageEstimator : uses
```

---

## 4. Agent 服务类图

### 4.1 Agent 构建与运行

```mermaid
classDiagram
    class IAgentBuilder {
        <<interface>>
        +GetOrCreateAgentAsync() Task~AIAgent~
        +InvalidateAsync() Task
        +GetContextWindowTokensAsync() Task~int~
        +GetCachedSystemPromptForTokenCount() string?
    }

    class IAgentContextFactory {
        <<interface>>
        +BuildSystemPromptAsync() Task~string~
        +GetCachedSystemPrompt() string?
    }

    class IToolProviderAggregator {
        <<interface>>
        +GetAllToolsAsync() Task~AITool[]~
    }

    class IMcpConnectionManager {
        <<interface>>
        +GetAllToolsAsync() Task~AITool[]~
        +InvalidateAsync() Task
    }

    class IModelConfigService {
        <<interface>>
        +GetDefaultAsync() Task~ModelConfig?~
        +GetAllAsync() Task~List~ModelConfig~~
        +SaveAllAsync() Task
    }

    class AgentBuilder {
        -AIAgent _agent
        -AITool[] _allTools
        -int _contextWindowTokens
        +GetOrCreateAgentAsync() Task~AIAgent~
        +InvalidateAsync() Task
    }

    class AgentRunnerAdapter {
        -IConversationRepository _repo
        -IAgentBuilder _agentBuilder
        -ITurnContextFragmentBuilder _fragmentBuilder
        -IContextWindowManager _contextWindowManager
        +RunStreamingAsync() IAsyncEnumerable~StreamUpdate~
        +GenerateTitleAsync() Task~string~
    }

    class AgentContextFactory {
        -string _cachedSystemPrompt
        +BuildSystemPromptAsync() Task~string~
        +GetCachedSystemPrompt() string?
    }

    IAgentBuilder <|.. AgentBuilder
    IAgentContextFactory <|.. AgentContextFactory
    AgentBuilder --> IAgentContextFactory : uses
    AgentBuilder --> IToolProviderAggregator : uses
    AgentBuilder --> IMcpConnectionManager : uses
    AgentBuilder --> IModelConfigService : uses
    AgentRunnerAdapter --> IAgentBuilder : uses
```

---

## 5. 工具系统类图

### 5.1 工具提供者

```mermaid
classDiagram
    class IToolProvider {
        <<interface>>
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
        +GetTimeout() TimeSpan?
    }

    class IToolProviderAggregator {
        <<interface>>
        +GetAllToolsAsync() Task~AITool[]~
    }

    class ToolProviderAggregator {
        -IEnumerable~IToolProvider~ _providers
        +GetAllToolsAsync() Task~AITool[]~
    }

    class TimeToolProvider {
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
    }

    class FileToolProvider {
        -IVirtualFileSystem _vfs
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
    }

    class ShellToolProvider {
        -ICommandRunner _runner
        -ICommandConfirmationService _confirmation
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
        +GetTimeout() TimeSpan?
    }

    class SearchToolProvider {
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
    }

    class TaskToolProvider {
        -ITaskListService _taskList
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
    }

    class SkillToolProvider {
        -ISkillsConfigService _skills
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
    }

    class ConversationToolProvider {
        -IConversationTaskContext _context
        +Name string
        +IsEnabled bool
        +GetTools() IEnumerable~AITool~
    }

    IToolProvider <|.. TimeToolProvider
    IToolProvider <|.. FileToolProvider
    IToolProvider <|.. ShellToolProvider
    IToolProvider <|.. SearchToolProvider
    IToolProvider <|.. TaskToolProvider
    IToolProvider <|.. SkillToolProvider
    IToolProvider <|.. ConversationToolProvider
    IToolProviderAggregator <|.. ToolProviderAggregator
    ToolProviderAggregator --> IToolProvider : aggregates
```

---

## 6. 仓储类图

### 6.1 仓储接口与实现

```mermaid
classDiagram
    class IConversationRepository {
        <<interface>>
        +CreateAsync() Task~Conversation~
        +GetListAsync() Task~List~Conversation~~
        +SearchAsync() Task~List~Conversation~~
        +GetByIdAsync() Task~Conversation?~
        +GetByIdNoUserCheckAsync() Task~Conversation?~
        +DeleteAsync() Task~bool~
        +GetMessageCountAsync() Task~int~
        +GetMessagesForConversationAsync() Task~List~ChatMessage~~
        +GetToolCallsForConversationAsync() Task~List~ToolCall~~
        +AddTurnAndUserMessageAsync() Task~Guid~
        +CompleteTurnWithAssistantAsync() Task
        +CompleteTurnWithErrorAsync() Task
        +ReplaceUserMessageAsync() Task
        +GetTurnForRegenerateAsync() Task
        +UpdateCompressionAsync() Task
    }

    class ConversationRepository {
        -SmallEBotDbContext _context
        +CreateAsync() Task~Conversation~
        +GetListAsync() Task~List~Conversation~~
        +SearchAsync() Task~List~Conversation~~
        +GetByIdAsync() Task~Conversation?~
        +GetByIdNoUserCheckAsync() Task~Conversation?~
        +DeleteAsync() Task~bool~
        +GetMessageCountAsync() Task~int~
        +GetMessagesForConversationAsync() Task~List~ChatMessage~~
        +GetToolCallsForConversationAsync() Task~List~ToolCall~~
        +AddTurnAndUserMessageAsync() Task~Guid~
        +CompleteTurnWithAssistantAsync() Task
        +CompleteTurnWithErrorAsync() Task
        +ReplaceUserMessageAsync() Task
        +GetTurnForRegenerateAsync() Task
        +UpdateCompressionAsync() Task
    }

    class SmallEBotDbContext {
        +DbSet~Conversation~ Conversations
        +DbSet~ChatMessage~ ChatMessages
        +DbSet~ToolCall~ ToolCalls
        +DbSet~ThinkBlock~ ThinkBlocks
        +DbSet~ConversationTurn~ ConversationTurns
    }

    IConversationRepository <|.. ConversationRepository
    ConversationRepository --> SmallEBotDbContext : uses
```

---

## 7. 工作区类图

### 7.1 虚拟文件系统

```mermaid
classDiagram
    class IVirtualFileSystem {
        <<interface>>
        +ReadFileAsync() Task~string~
        +WriteFileAsync() Task
        +AppendFileAsync() Task
        +ListFilesAsync() Task~List~string~~
        +DeleteFileAsync() Task
        +FileExistsAsync() Task~bool~
        +CopyDirectoryAsync() Task
    }

    class IWorkspaceService {
        <<interface>>
        +GetWorkspaceRoot() string
        +EnsureWorkspaceExists() Task
    }

    class IWorkspaceWatcher {
        <<interface>>
        +StartWatching() void
        +StopWatching() void
        +event FileChangedEventHandler FileChanged
    }

    class VirtualFileSystem {
        -string _rootPath
        +ReadFileAsync() Task~string~
        +WriteFileAsync() Task
        +AppendFileAsync() Task
        +ListFilesAsync() Task~List~string~~
        +DeleteFileAsync() Task
        +FileExistsAsync() Task~bool~
        +CopyDirectoryAsync() Task
        -GetFullPath() string
        -ValidatePath() void
    }

    class WorkspaceService {
        -string _rootPath
        +GetWorkspaceRoot() string
        +EnsureWorkspaceExists() Task
    }

    class WorkspaceWatcher {
        -FileSystemWatcher _watcher
        -string _rootPath
        +StartWatching() void
        +StopWatching() void
        +event FileChangedEventHandler FileChanged
    }

    IVirtualFileSystem <|.. VirtualFileSystem
    IWorkspaceService <|.. WorkspaceService
    IWorkspaceWatcher <|.. WorkspaceWatcher
```

---

## 8. UI 状态类图

### 8.1 ChatState 与视图模型

```mermaid
classDiagram
    class ChatState {
        +IReadOnlyList~ChatBubble~ Bubbles
        +Guid ConversationId
        +string InputText
        +IReadOnlyList~AttachmentItem~ Attachments
        +IReadOnlyList~string~ RequestedSkillIds
        +bool IsStreaming
        +DateTime StreamingStartedAt
        +string StreamingText
        +IReadOnlyList~StreamUpdate~ StreamingUpdates
        +bool PopoverOpen
        +string ContextPercentText
        +event Action StateChanged
        +event Func~Task~ MessageSent
        +SetBubbles() void
        +SetConversationId() void
        +SetInputText() void
        +AddAttachment() void
        +StartStreaming() void
        +StopStreaming() void
        +AddStreamUpdate() void
    }

    class BubbleViewBase {
        <<abstract>>
        +Guid Id
        +DateTime CreatedAt
    }

    class UserBubbleView {
        +string Content
        +IReadOnlyList~string~ AttachedPaths
        +IReadOnlyList~string~ RequestedSkillIds
    }

    class AssistantBubbleView {
        +string Content
        +IReadOnlyList~ReasoningStepView~ ReasoningSteps
        +IReadOnlyList~SegmentBlockView~ Segments
    }

    class ReasoningStepView {
        +Guid Id
        +string Content
        +bool IsExpanded
    }

    class SegmentBlockView {
        +string Content
        +bool IsThink
    }

    class StreamingDisplayItemView {
        +StreamUpdate Update
        +int Index
    }

    class ChatPresentationService {
        +ConvertToBubbleViews() List~BubbleViewBase~
        +ConvertStreamingUpdates() List~StreamingDisplayItemView~
    }

    BubbleViewBase <|-- UserBubbleView
    BubbleViewBase <|-- AssistantBubbleView
    ChatState --> ChatBubble : contains
    ChatPresentationService --> BubbleViewBase : creates
    ChatPresentationService --> StreamingDisplayItemView : creates
```

---

## 9. 终端服务类图

### 9.1 命令执行与确认

```mermaid
classDiagram
    class ICommandRunner {
        <<interface>>
        +RunAsync() Task~CommandResult~
    }

    class ICommandConfirmationService {
        <<interface>>
        +WaitForConfirmationAsync() Task~CommandConfirmResult~
        +event EventHandler~PendingRequestEventArgs~ ConfirmationRequested
    }

    class ITerminalConfigService {
        <<interface>>
        +IsDangerousCommand() bool
        +GetBlacklist() List~string~
    }

    class CommandRunner {
        -ITerminalConfigService _config
        +RunAsync() Task~CommandResult~
    }

    class CommandConfirmationService {
        -ConcurrentDictionary~string, PendingCommandRequest~ _pending
        +WaitForConfirmationAsync() Task~CommandConfirmResult~
        +CompleteRequest() void
        +event ConfirmationRequested
    }

    class TerminalConfigService {
        -List~string~ _blacklist
        +IsDangerousCommand() bool
        +GetBlacklist() List~string~
    }

    class CommandConfirmResult {
        <<enumeration>>
        Allowed
        Denied
    }

    class PendingCommandRequest {
        +string Command
        +string ContextId
        +TaskCompletionSource~CommandConfirmResult~ Tcs
    }

    ICommandRunner <|.. CommandRunner
    ICommandConfirmationService <|.. CommandConfirmationService
    ITerminalConfigService <|.. TerminalConfigService
    CommandRunner --> ITerminalConfigService : uses
    CommandConfirmationService --> PendingCommandRequest : manages
    CommandConfirmationService --> CommandConfirmResult : returns
```

---

## 10. 依赖注入配置类图

### 10.1 服务注册关系

```mermaid
classDiagram
    class ServiceCollectionExtensions {
        +AddSmallEBotHostServices() IServiceCollection
    }

    class IServiceCollection {
        <<interface>>
    }

    class ServiceDescriptor {
        +Type ServiceType
        +Type ImplementationType
        +ServiceLifetime Lifetime
    }

    class ServiceLifetime {
        <<enumeration>>
        Singleton
        Scoped
        Transient
    }

    ServiceCollectionExtensions --> IServiceCollection : configures
    IServiceCollection --> ServiceDescriptor : contains
    ServiceDescriptor --> ServiceLifetime : has
```

**服务注册说明：**

| 服务 | 生命周期 |
|------|----------|
| DbContext | Scoped |
| Repository | Scoped |
| IAgentConversationService | Scoped |
| IAgentBuilder | Scoped |
| IToolProvider[] | Singleton/Scoped |
| ChatState | Scoped |
| IMcpConnectionManager | Singleton |
